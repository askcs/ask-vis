extends layout

block body
  .container-fluid(ng-controller='AppCtrl')

    .row
      .col-md-11.col-md-offset-1
        h2.pull-left Timeline

    .row
      .well.col-md-10.col-md-offset-1
        time-board(timeline='timeline')
          span {{timeline.info}}
          // span {{testing}}
          //br
          //span Info: {{timeline.range | filterRangeInfo}}

        time-nav(timeline='timeline')

          .btn-group.pull-right
            button.btn.btn-default(ng-click='timeline.stepScope(-1)') <
            button.btn.btn-default(ng-click='timeline.stepScope(1)') >
          .btn-group.pull-right(style='margin-right: 15px')
            button.btn.btn-default(ng-click='timeline.setScope("day")',
            class='{{(timeline.scope.day) && "active"}}') D
            button.btn.btn-default(ng-click='timeline.setScope("week")',
            class='{{(timeline.scope.week) && "active"}}') W
            button.btn.btn-default(ng-click='timeline.setScope("month")',
            class='{{(timeline.scope.month) && "active"}}') M
            button.btn.btn-default(ng-click='timeline.setScope("year")',
            class='{{(timeline.scope.year) && "active"}}') Y

    .row
      .col-md-10.col-md-offset-1
        time-line(items='items', timeline='timeline')

    .row

      .col-md-7.col-md-offset-1
        br

        form.form-inline(role='form')
          .form-group(style='margin-right: 15px')
            label.sr-only Custom time:
            input.form-control(type='date',
            ng-model='customDate')
          button.btn.btn-default(type='submit',
          style='margin-right: 15px',
          ng-click='timeline.setCustomTime(customDate)') Set custom time
          span {{timeline.customTime}}
        br

        button.btn.btn-default(ng-click='getCustomTime()',
        style='margin-right: 15px') Get Custom Time
        span {{gotCustomDate}}
        br
        br

        form.form-inline(role='form')
          .form-group(style='margin-right: 15px')
            label.sr-only Selection:
            input.form-control(type='text',
            ng-model='selection')
          .btn-group(style='margin-right: 15px')
            button.btn.btn-default(type='submit',
            ng-click='setSelection(selection)') Set selection
            button.btn.btn-default(ng-click='getSelection()',
            style='margin-right: 15px') Get Selection
          span {{gotSelection}}
        br

        form.form-inline(role='form')
          .form-group(style='margin-right: 15px')
            label.sr-only Start:
            input.form-control(type='date',
            ng-model='start')
          .form-group(style='margin-right: 15px')
            label.sr-only End:
            input.form-control(type='date',
            ng-model='end')
          button.btn.btn-default(type='submit',
          style='margin-right: 15px',
          ng-click='setWindow(start, end)') Set window
        br

        button.btn.btn-default(ng-click='getWindow()',
        style='margin-right: 15px') Get Window
        span {{gotWindow}}
        br
        br

        .btn-group
          button.btn.btn-default(ng-click='setOptions({orientation: "top"})')
            | Timeline header top
          button.btn.btn-default(ng-click='setOptions({orientation: "bottom"})')
            | Timeline header bottom

      .col-md-3
        h3 Groups
        hr
        div(ng-repeat='(group, items) in items')
          h4 {{group}}
          ul
            li(ng-repeat='item in items')
              a(ng-click='setSelection(item.id)')
                | {{item.id}}: {{item.content}}, {{item.start}} - {{item.end}}

block live-reload
  script(src='http://localhost:35729/livereload.js?snipver=1')